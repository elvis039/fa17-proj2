<% if user_signed_in? %>

  <% @movies.each do |m| %>
    <%= link_to "#{m.title}", movie_path(m) %>
  <% end %>
<% else %>
  <%= link_to "Log In", new_user_session_path %></br>
  <%= link_to "Please signup before proceeding (this is placeholder text)", new_user_registration_path %>
<% end %>